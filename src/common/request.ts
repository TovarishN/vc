export type RegisterInfo = {
    username: string;
    firstname: string;
    lastname: string;
    email: string;
    password: string;
};

export type LoginInfo = {
    username: string;
    password: string;
};